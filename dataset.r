
###This is just to simulate the data and see if seeting a seed

##Define the variables first.
n=1000
set.seed(1234)
x=runif(n, min=-sqrt(3), max=sqrt(3))
epsilon= runif(n, min=-0.5, max=0.5)
theta=2
